def rotate(rotate_direction: float):
    """
    Gets a float and rotates the robot either left of right according to it
    """
    print(f"Rotating robot: {rotate_direction}")


def set_velocity(velocity_strength: float):
    """Sets the velocity to the inputted strength"""
    print(f"Setting velocity to {velocity_strength}")
